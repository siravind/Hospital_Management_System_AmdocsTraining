--CREATE TABLE Patient (
--    patientId VARCHAR2(20) PRIMARY KEY,
--    fullname VARCHAR2(30) NOT NULL,
--    pwd VARCHAR2(20) NOT NULL,
--    dob date NOT NULL, 
--    govtid VARCHAR2(20) NOT NULL,
--    gender VARCHAR2(10) NOT NULL,
--    approvalStatus NUMBER(1) DEFAULT 0,
--    registrationTime DATE NOT NULL
--);
--  
--CREATE TABLE Doctor (
--    doctorId VARCHAR2(20) PRIMARY KEY,
--    fullname VARCHAR2(30) NOT NULL,
--    pwd VARCHAR2(20) NOT NULL,
--    dob date NOT NULL,
--    govtid VARCHAR2(20) NOT NULL,
--    gender VARCHAR2(10) NOT NULL,
--    approvalStatus NUMBER(1) DEFAULT 0,
--    fees INT NOT NULL,
--    perSlot INT NOT NULL,
--    registrationTime DATE NOT NULL
--);
--
----ALTER TABLE Patient 
----ADD registrationTime DATE NOT NULL;
--
----CREATE TABLE Department (
----    deptId INT PRIMARY KEY,
----    deptname VARCHAR2(20) NOT NULL,
----    des VARCHAR2(50),
----    address VARCHAR2(20)
----);
--
--CREATE TABLE Appointments (
--    apptId INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    patientId INT NOT NULL,
--    doctorId INT NOT NULL,
--    doa date NOT NULL,
--    slot INT NOT NULL,
--    payment NUMBER(1) DEFAULT 0,
--    prescription VARCHAR2(100)
--);
--
--CREATE TABLE Slots(
--    slotId INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--    doctorId INT NOT NULL,
--    dateofSlot DATE NOT NULL,
--    slot INT NOT NULL
--);

ALTER TABLE Doctor
ADD deptId INT NOT NULL;

ALTER TABLE Doctor
ADD deptname VARCHAR2(30);
--ALTER TABLE Doctor
--ADD CONSTRAINT fk_deptId
--FOREIGN KEY (deptId)
--REFERENCES Department(deptId);
--
--DROP TABLE Doctor;
--
--drop table Slots;
--
--drop table Patient;
--
--drop table appointments;
--
--drop table Slots

--DELETE FROM department WHERE deptid = 1;

--INSERT INTO department (deptid, deptname, des, address) VALUES
--(1, 'Cardiology', 'Heart and vascular treatment', 'Floor 1');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(2, 'Neurology', 'Brain and nervous system treatment', 'Floor 2');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(3, 'Orthopedics', 'Bone and joint treatment', 'Floor 3');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(4, 'Pediatrics', 'Child healthcare', 'Floor 4');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(5, 'Gynecology', 'Women’s health', 'Floor 5');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(6, 'Dermatology', 'Skin treatment', 'Floor 6');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(7, 'Oncology', 'Cancer treatment', 'Floor 7');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(8, 'Radiology', 'Imaging services', 'Floor 8');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(9, 'Gastroenterology', 'Digestive system treatment', 'Floor 9');
--INSERT INTO department (deptid, deptname, des, address) VALUES 
--(10, 'Emergency', 'Emergency services', 'Ground Floor');



